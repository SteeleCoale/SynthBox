// import styled from 'styled-components';
import React from 'react';

const LogBar = ({ x, y }) => {
	return (
		<>
			<p>X Location: {x}</p>
			<p>Y Location: {y}</p>
		</>
	);
};

export default LogBar;
